; AGS2 Music Start Script
; -----------------------

; Skip end, if ags2 menu path doesn't exist
IF NOT EXISTS ENV:ags2menupath
  SKIP end
ENDIF

; Skip end, if music directory doesn't exist
IF NOT EXISTS "$ags2menupath/music"
  SKIP end
ENDIF

; Skip end, if ags2music directory doesn't exist
IF NOT EXISTS "ENV:ags2music"
  SKIP end
ENDIF

; Pick random music file from ags2 menu path music directory
setenv randommusic `C:filepick "$ags2menupath/music/#?"`

; Use easymod to play random music file in background, if any exists
IF NOT $randommusic EQ ""
  run >NIL: C:easymod >NIL: "$randommusic"
ENDIF

; End
LAB end
