; Hst Launcher run script
; -----------------------
;RunFile=A-Games:S/SorcererLord/SorcererLord.Slave

; Run WHDLoad
cd "A-Games:S/SorcererLord/"
IF EXISTS ENV:whdloadargs
  whdload "SorcererLord.Slave" $whdloadargs
ELSE
  whdload "SorcererLord.Slave"
ENDIF
