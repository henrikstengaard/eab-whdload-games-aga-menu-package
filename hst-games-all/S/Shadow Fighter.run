; Hst Launcher run script
; -----------------------
;RunFile=A-Games:S/ShadowFighter/ShadowFighter.Slave

; Run WHDLoad
cd "A-Games:S/ShadowFighter/"
IF EXISTS ENV:whdloadargs
  whdload "ShadowFighter.Slave" $whdloadargs
ELSE
  whdload "ShadowFighter.Slave"
ENDIF
