; Hst Launcher run script
; -----------------------
;RunFile=A-Games:L/LeavinTeramis/LeavinTeramis.Slave

; Run WHDLoad
cd "A-Games:L/LeavinTeramis/"
IF EXISTS ENV:whdloadargs
  whdload "LeavinTeramis.Slave" $whdloadargs
ELSE
  whdload "LeavinTeramis.Slave"
ENDIF
