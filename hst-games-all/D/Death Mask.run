; Hst Launcher run script
; -----------------------
;RunFile=A-Games:D/DeathMask/DeathMask.Slave

; Run WHDLoad
cd "A-Games:D/DeathMask/"
IF EXISTS ENV:whdloadargs
  whdload "DeathMask.Slave" $whdloadargs
ELSE
  whdload "DeathMask.Slave"
ENDIF
