; Hst Launcher run script
; -----------------------
;RunFile=A-Games:A/Atax/Atax.Slave

; Run WHDLoad
cd "A-Games:A/Atax/"
IF EXISTS ENV:whdloadargs
  whdload "Atax.Slave" $whdloadargs
ELSE
  whdload "Atax.Slave"
ENDIF
