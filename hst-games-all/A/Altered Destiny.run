; Hst Launcher run script
; -----------------------
;RunFile=A-Games:A/AlteredDestiny/AlteredDestiny.slave

; Run WHDLoad
cd "A-Games:A/AlteredDestiny/"
IF EXISTS ENV:whdloadargs
  whdload "AlteredDestiny.slave" $whdloadargs
ELSE
  whdload "AlteredDestiny.slave"
ENDIF
