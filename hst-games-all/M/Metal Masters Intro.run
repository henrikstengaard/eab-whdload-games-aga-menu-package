; Hst Launcher run script
; -----------------------
;RunFile=A-Games:M/MetalMasters/MetalMastersIntro.Slave

; Run WHDLoad
cd "A-Games:M/MetalMasters/"
IF EXISTS ENV:whdloadargs
  whdload "MetalMastersIntro.Slave" $whdloadargs
ELSE
  whdload "MetalMastersIntro.Slave"
ENDIF
