; Hst Launcher run script
; -----------------------
;RunFile=A-Games:K/Killerball/Killerball.Slave

; Run WHDLoad
cd "A-Games:K/Killerball/"
IF EXISTS ENV:whdloadargs
  whdload "Killerball.Slave" $whdloadargs
ELSE
  whdload "Killerball.Slave"
ENDIF
