; Hst Launcher run script
; -----------------------
;RunFile=A-Beta:N/NightWalk/NightWalk.Slave

; Run WHDLoad
cd "A-Beta:N/NightWalk/"
IF EXISTS ENV:whdloadargs
  whdload "NightWalk.Slave" $whdloadargs
ELSE
  whdload "NightWalk.Slave"
ENDIF
