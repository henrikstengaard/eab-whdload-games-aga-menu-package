; Hst Launcher run script
; -----------------------
;RunFile=A-Games:C/CaptainDynamo/CaptainDynamo.slave

; Run WHDLoad
cd "A-Games:C/CaptainDynamo/"
IF EXISTS ENV:whdloadargs
  whdload "CaptainDynamo.slave" $whdloadargs
ELSE
  whdload "CaptainDynamo.slave"
ENDIF
